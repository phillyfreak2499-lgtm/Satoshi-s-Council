# Satoshi's Council — runtime dependencies
# Build:  pip install -r requirements.txt
# Start:  PYTHONPATH=. uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1

# Web
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
orjson>=3.10,<4.0            # ORJSONResponse (default_response_class)
pydantic-settings>=2.4,<3.0  # backend/config.py Settings
python-dotenv>=1.0,<2.0      # .env → os.environ for local secret loading

# Storage
SQLAlchemy[asyncio]>=2.0,<3.0
aiosqlite>=0.20,<1.0         # sqlite+aiosqlite DATABASE_URL

# Data feeds
httpx>=0.27,<1.0             # Binance / Kalshi / CoinGlass clients
numpy>=1.26,<3.0             # agent math

# Kalshi trade signing — only needed when kalshi_trade.py is a real client.
# cryptography>=42.0

# Excel export (GET /api/admin/export.xlsx)
openpyxl>=3.1,<4.0

# Logging
loguru>=0.7,<1.0

# zoneinfo needs a tz database. Windows has none, and slim Linux images
# often drop it — ZoneInfo("America/Chicago") raises without this, which
# takes down import of huddle / follower_gate / desk_side / desk_front / db.
tzdata>=2024.1


# stripe removed — paid membership is disabled
