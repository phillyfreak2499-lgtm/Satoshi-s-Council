Satoshi's Council — Fresh Deploy (GitHub + Render)

THIS BUILD INCLUDES (newest fixes):
- Hard mute toxic bots (CHEAP/PANIC under 40% WR with 40+ samples)
- Near-certain freebies only get 25% learning credit (entry >= 88%)
- Higher flip penalty (180s post-miss, 90s re-entry)
- Early/late window discipline (~first 90s + last ~3 min mostly WAIT)
- Full config with CONFLUENCE_FLOOR and all required settings
- Path grading, adaptive learning, hierarchy, LAW, huddle
- Password gate, SATOSHI chair, LOCKED mode, videos, logo

================================================
1. UNZIP
================================================
Extract this zip. You must see at the TOP level:
  backend/   frontend/   requirements.txt   render.yaml
(NOT another nested satoshi-council folder)

================================================
2. NEW GITHUB REPO
================================================
- github.com/new
- Name: Satoshi-Council (or any name)
- Do NOT add README / .gitignore / license
- Create

================================================
3. UPLOAD
================================================
On the empty repo page: Add file → Upload files
Drag ALL contents from the unzipped folder
Commit message: Fresh council with toxic-bot mute + flip discipline
Wait for videos to finish uploading

VERIFY on GitHub: requirements.txt and backend/ are at the ROOT.

================================================
4. RENDER
================================================
New → Web Service → connect the repo

Build Command:
  pip install -r requirements.txt

Start Command:
  PYTHONPATH=. uvicorn backend.main:app --host 0.0.0.0 --port $PORT --workers 1

Environment:
  DATA_DIR=/opt/render/project/src/data
  DATABASE_URL=sqlite+aiosqlite:////opt/render/project/src/data/council.db
  CORS_ORIGINS=*
  BEAST_MODE=true
  ANALYSIS_INTERVAL=2

Disk (recommended):
  Mount: /opt/render/project/src/data
  Size: 1 GB

Health Check: /health
Instance: Starter

================================================
5. AFTER LIVE
================================================
Open the Render URL
Set COUNCIL_ACCESS_PASSWORD in the Render Dashboard (Environment). Do not put the value in this file.
Hard refresh (Ctrl+Shift+R)

If you have an old brain.json from before, import it in Settings
so you keep lifetime learning.

CHEAP and PANIC should now show as hard-muted / inverted
once they still have 40+ samples with WR under 40%.
